name: publish

on: [push]

jobs:
  publish-bwalink-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the bwalink Docker image
      run: |
        echo $CR_PAT | docker login ghcr.io -u jshank --password-stdin
      env:
        CR_PAT: ${{ secrets.CR_PAT }}